{
  "name": "spritez",
  "version": "0.1.0",
  "description": "Simple sprite implementation to draw into any canvas.",
  "main": "src/sprite.ts",
  "scripts": {
    "samples": "npx browser-sync start --server --index ./samples/index.html --files ./samples/*",
    "dev": "npx webpack --config webpack.config.js --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["animation", "canvas", "game"],
  "author": "Renan S Moreira",
  "license": "MIT",
  "dependencies": {
    "typescript": "^4.3.4"
  },
  "devDependencies": {
    "browser-sync": "^2.27.5",
    "ts-loader": "^9.2.5",
    "webpack": "^5.53.0",
    "webpack-cli": "^4.8.0"
  }
}
